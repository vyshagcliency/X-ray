# Add these to the new repo's .gitignore

# Claude local settings and memory (personal, never committed)
.claude/settings.local.json
.claude/memory/
.claude/agent-memory-local/
.claude/plans/

# Environment
.env
.env.local
.env.*.local

# Supabase local
supabase/.temp/
supabase/seed.sql   # if it contains real data

# DuckDB temp files
*.duckdb
*.duckdb.wal

# Parquet test fixtures with real data (synthetic ones are fine to commit)
tests/fixtures/real-*.parquet
